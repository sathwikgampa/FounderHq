# `/config` Directory

## Overview

Shared environment configuration files, cloud deployment manifests, and security policy rules.

## Directory Contents

- `firebase.json`: Firebase Emulators and service parameters configuration.
- `firestore.rules`: Security rules for Firestore multi-tenant data access control.
- `storage.rules`: Security rules for Firebase Cloud Storage asset uploads.
