rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default security rule: deny all unauthenticated read/write access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
